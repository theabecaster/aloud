import SwiftUI

extension Color {
    /// Aloud's own blue, sampled from the app icon (#0070E0). Used where
    /// something should read as *the app doing something*, rather than
    /// borrowing the user's system accent colour.
    static let aloud = Color(red: 0x00 / 255, green: 0x70 / 255, blue: 0xE0 / 255)
}
